<div class="flex flex-col min-w-3xs bg-sky-100">
  <h1 class="text-xl font-bold p-6 text-white bg-sky-700">Books Inventory</h1>
  <div class="flex flex-col m-6 gap-6">
    <% categories.forEach((category) => { %> <%- include('categoryButton',
    {category: category}); %> <% }) %>
    <a
      class="text-center text-white bg-sky-500 p-2 rounded-sm hover:bg-sky-600"
      href="<%= addNewUrl %>"
    >
      Add Genre
    </a>
  </div>
</div>
